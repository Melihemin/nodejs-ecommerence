<div class="listContainer">
        <div class="list-group">
          {{#each categories}}
          <a href="/ilanlar/kategori/{{_id}}" class="list-group-item list-group-item-action">{{cname}} <span>({{num_of_arsas}})</span></a>
          {{/each}}
        </div>
        <div class="vertical-line"></div>
</div>